export const environment = {
  production: true,
  apiUrl: 'http://localhost:9998/api' // when you release your app to production, this should change to whatever host or address your API will be deployed to
};
